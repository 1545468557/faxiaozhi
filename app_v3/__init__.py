"""法小智 v3 后端（重写版）

与旧 `app/` 的关系：
- **不复用**旧的产品逻辑：四块答案、追问计数状态机、引用核验门禁、SSE 单会话事件队列，全部不要。
- **复用**两处纯管道：`app.config`（配置与环境变量）与 `app.tools.mcp.McpProvider`（北大法宝 12 个端点的
  调用与字段映射）。这两处重写一遍只是重新踩坑，不产生任何产品价值。

设计口径：
- 一次请求 = 一轮对话；回答**流式**吐字（不再是"一次运行一条 SSE、整段返回"）。
- 会话与消息**落 SQLite**：刷新、切页、重启服务都能恢复（旧版重启即丢）。
- 检索到的法条/案例作为**材料**喂给模型并推给界面**仅作参考链接**，不做逐条核验。
"""
