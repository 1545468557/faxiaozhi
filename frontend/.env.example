# 这个模板只保留一项配置：后端地址（不含任何密钥）
# 复制为 .env.local 使用；不得加 NEXT_PUBLIC_ / VITE_ 前缀。
#
# 模型密钥与北大法宝 Token **只放在后端**（projects/法小智agent开发文档/.env），
# 前端工程里永远不出现密钥（AGENTS.md 底线 1）。
BACKEND_BASE_URL=http://127.0.0.1:8010

# 首页新版法律问答后端
V3_BACKEND_BASE_URL=http://127.0.0.1:8011
